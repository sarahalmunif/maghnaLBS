/**
 * The initial values for the redux state.
 */
const initialState = {
    homeSwitches: {
        'toggle1': false,
        'toggle2': false,
        'toggle3': false,
        'toggle4': false,
    }
}

export default initialState;
